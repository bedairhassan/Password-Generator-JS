Types: value,special_character,string

1. Dynamic Use => Programmer using this function can use array of n arrays
2. You never see the same 2 types consequently. for example, NEVER value, value // display HUGE on logging screen to understand me
3. Same character never gets repeated in string ! letter a is produced once for entire password
